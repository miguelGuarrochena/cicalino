export const ThemedImg = ({
  name,
  alt = "",
  className = "",
}: {
  name: string;
  alt?: string;
  className?: string;
}) => {
  return (
    <>
      {}
      <img
        src={`/${name}-light.png`}
        alt={alt}
        className={`on-light ${className}`}
      />
      {}
      <img
        src={`/${name}-dark.png`}
        alt={alt}
        className={`on-dark ${className}`}
      />
    </>
  );
};
